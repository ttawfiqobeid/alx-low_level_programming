#!/bin/bash

# Check if the CFILE variable is set
if [ -z "$CFILE" ]; then
    echo "CFILE is not set."
    exit 1
fi

# Run the C file through the preprocessor and save the result in a file named "c"
gcc -E "$CFILE" -o c

