#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints a specific text followed by a newline character
 * to the standard output.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("Programming is like building a multilingual puzzle");
	return (0);
}

